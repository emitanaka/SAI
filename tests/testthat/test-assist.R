test_that("tools works", {
  # sai_assist("get the current time",
  #            tools = list(tool_register("get_current_time",
  #                                       "Get the current time",
  #                                       tool_parameters(tz = tool_prop("AEST", enum = "minutes")))))
})
